import 'package:flutter/widgets.dart';

class YedidimThanks extends StatelessWidget {
  const YedidimThanks({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      // color: Colors.blue,
      child: const Image(
        image: AssetImage('images/full_hack.png'),
      ),
    );
  }
}
